import { StyleSheet, css } from 'elements'
import { useMemo } from 'react'

const useStyles = () => useMemo(() => {
    return StyleSheet.create({
        container: {
            
        }
    })
}, []);

export default useStyles;